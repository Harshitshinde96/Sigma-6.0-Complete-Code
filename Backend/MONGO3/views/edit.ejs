<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit your Chat</title>
  </head>
  <body>
    <h3>Edit your Chat : <%= chat._id %></h3>

    <p>This chat was send from <b><%= chat.from %> to <%= chat.to %></b></p>

    <form action="/chats/<%= chat._id %>?_method=PUT" method="POST">
      <textarea name="msg" rows="6" cols="50"><%= chat.message %></textarea>
      <br />
      <button>Edit</button>
    </form>
  </body>
</html>
